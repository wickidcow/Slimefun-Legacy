# Commands

The maintained Slimefun Legacy command reference is available here:

➡️ **[Commands & Permissions](Commands-and-Permissions.md)**

Useful operator diagnostics include `/sf versions` and the `/sf doctor` command family. Player-facing command availability can also depend on permissions and server configuration.
